<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.battleGeneratorActivity"></activity>
        <activity android:name=".activities.eventsActivity" />
        <activity android:name=".activities.bandsActivity" />
        <activity android:name=".activities.deploymentActivity" />
        <activity android:name=".activities.questsActivity" />
        <activity android:name=".activities.mapsActivity" />
        <activity android:name=".activities.mainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>